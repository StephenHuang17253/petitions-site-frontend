type User = {
    id: number,
    firstName: string,
    lastName: string,
    email: string,
    password: string,
    imageFilename: string,
    authToken: string
}

type UserInfo = {
    firstName: string,
    lastName: string,
    email: string,
}